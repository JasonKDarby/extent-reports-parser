language: groovy

jdk:
  - oraclejdk8

script:
  - ./gradlew check allSauceLabsTests

after_success:
  - bash <(curl -s https://codecov.io/bash)

addons:
  sauce_connect: true

before_cache:
  - rm -f $HOME/.gradle/caches/modules-2/modules-2.lock
cache:
  directories:
    - $HOME/.gradle/caches/
    - $HOME/.gradle/wrapper/
